# MySCUT 产品语境

本文统一 MySCUT 产品讨论和 issue 中使用的领域词汇，避免同一用户体验使用多个名称。产品目标与工程边界见 [PROJECT_BASIS.md](PROJECT_BASIS.md)。

## Language

**首次使用导览页**：
课表库为空且不在课表交集预览时，替换课程内容区的功能导览。它复用应用图标，展示三项核心能力和一个进入课表设置的“导入课表”主行动；显示期间隐藏日期、周次和课表操作，保留底部主导航，不记录“是否看过”的额外状态。
_Avoid_: 欢迎页、多页 onboarding、空网格提示

**品牌启动壳**：
应用等待本地存储初始化时立即显示的轻量品牌界面。它只负责表达启动进度；初始化失败仍进入现有只读降级流程，不在启动壳中复制错误恢复。
_Avoid_: 启动错误页、第二套存储恢复流程

**页面恢复状态**：
路由内容渲染异常时替换该内容区的恢复界面，主导航尽量保留。它提供重试当前页面和返回课程页，生产环境不向用户展示堆栈或原始响应。
_Avoid_: 全应用崩溃页、完整技术错误页

**华工教务 PDF 课表**：
华工教务系统固定版式导出的可选中文本 PDF。当前只识别单页、横向 A4 的学生个人课表，不尝试 OCR、图片 PDF、其他学校格式或通用表格推断；具体边界见 [SCUT_PDF_LAYOUT.md](docs/SCUT_PDF_LAYOUT.md)。
_Avoid_: 任意课表 PDF、通用 PDF 识别

**AI 决策验证**：
针对维护者托管 AI 的匿名滥用、成本上限、访问边界、隐私和人机验证体验进行有时限的验证，并以 Go/No-Go 结束。当前结论已为 No-Go，继续使用 BYOK；只有满足 [ADR 0002](docs/adr/0002-keep-ai-byok-without-hosted-workers-ai.md) 的触发条件才重新评估。未来 Go 只授权另开实现 issue，不代表直接上线。
_Avoid_: Workers AI 实现、无限期技术调研

## Example dialogue

- 产品：课表库为空时显示首次使用导览页。
- 开发：导入并保存第一份课表后，课程首页改为显示该课表；删除全部课表后，首次使用导览页再次出现。
